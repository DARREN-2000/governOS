name: Helm Lint & Template

on:
  push:
    branches: [main]
    paths:
      - 'infra/helm/**'
  pull_request:
    branches: [main]
    paths:
      - 'infra/helm/**'

permissions:
  contents: read

jobs:
  helm-lint:
    name: Helm Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: azure/setup-helm@v5
        with:
          version: v3.14.0

      - name: Lint chart
        run: helm lint infra/helm/governos

      - name: Template chart
        run: helm template governos infra/helm/governos

      - name: Template with custom values
        run: |
          helm template governos infra/helm/governos \
            --set replicaCount=3 \
            --set ingress.enabled=true \
            --set autoscaling.enabled=true
