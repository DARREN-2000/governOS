<div align="center">

# GovernOS (formerly IntentGraph)

**The Trust Layer for Autonomous Agents**

[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](https://opensource.org/licenses/MIT)
[![CI Status](https://github.com/organization/intentgraph/workflows/CI/badge.svg)](https://github.com/organization/intentgraph/actions)
[![Python Version](https://img.shields.io/badge/python-3.10%2B-blue)](https://www.python.org/)
[![Node Version](https://img.shields.io/badge/node-20%2B-green)](https://nodejs.org/)

GovernOS orchestrates complex, cross-service workflows generated by LLMs while enforcing strict security, compliance, and cost boundaries. It turns natural-language goals into deterministic, machine-queryable execution plans that require human approval for risky actions.

[**Documentation**](https://docs.governos.io) | [**API Reference**](https://docs.governos.io/api/reference) | [**Demo**](https://DARREN-2000.github.io/IntentGraph/)

</div>

---

## 📖 Project Overview

GovernOS is an orchestration engine and multi-tenant action OS for enterprise AI agents. While LLMs are excellent at reasoning, they cannot be trusted to execute side-effects directly. GovernOS solves this by introducing a **deterministic trust layer** between the LLM planner and the execution environment.

### Product Vision

To become the standard infrastructure for safe agentic execution in the enterprise, ensuring that AI can operate autonomously without violating security perimeters or spending constraints.

### Key Features

- **Preview Before Execute:** LLM intents are compiled into typed Execution Plans. You always see exactly what will happen before it does.
- **Policy Enforcement:** Deterministic rules block risky plans (e.g., spending over $100, deleting production databases).
- **Human-in-the-Loop:** Asynchronous approval workflows for actions flagged by the policy engine.
- **Idempotent Execution:** Built-in support for retries, state recovery, and automatic rollbacks via the `compensate()` contract.
- **Contextual Memory:** Scoped context (Personal, Project, Organization) prevents data leakage between tenants.
- **Rich Web Dashboard:** A premium, developer-first interface for tracking workflow state and approving actions.

---

## 🏛️ Architecture Overview

GovernOS separates planning from execution, connected by an approval state machine.

```text
User Intent (Web / CLI)
  -> Planner Service (intent -> workflow spec)
  -> Executor Service (policy + approvals + runtime)
  -> Action Plugins (preview/execute/compensate)
  -> Audit Service (event trail + integrity)
  -> Memory Service (scoped context)
```

**Why This Exists:** Most agent frameworks (like LangChain or AutoGen) assume the LLM directly calls tools. GovernOS is designed for scenarios where the "tool" is a destructive API call (e.g., dropping a table, sending a wire transfer) that requires auditing and authorization.

**Comparison With Alternatives:**
- *vs. LangChain/LlamaIndex:* GovernOS is an orchestration layer, not an LLM application framework. It manages the lifecycle of the *action*, not the prompt.
- *vs. Temporal/Airflow:* GovernOS uses similar durability concepts but is purpose-built for dynamic, LLM-generated directed acyclic graphs (DAGs) rather than static, predefined pipelines.

---

## 📷 Experience

<div align="center">
  <img src="docs/screenshots/dashboard-workflow-planned.png" width="45%" alt="Workflow Planned" />
  <img src="docs/screenshots/dashboard-execution-complete.png" width="45%" alt="Execution Complete" />
</div>

*Note: You can view more screenshots and videos in the `docs/screenshots` and `docs/videos` directories.*

---

## 🛠️ Technology Stack

- **Frontend:** React 19, TypeScript, Vite, Tailwind CSS, shadcn/ui.
- **Backend (Core Analysis):** Python 3.10+, NetworkX, Pydantic, AST.
- **Backend (Services):** Node.js 20+, Better-SQLite3, Temporal (Durability), Express.
- **Infrastructure:** Docker, Kubernetes (Helm), Terraform.

### Project Structure

GovernOS is a monorepo containing both the frontend applications and backend core.

```text
IntentGraph/
├── apps/
│   ├── api/            # Node.js + Express + SQLite Backend
│   └── web/            # React + Vite Dashboard
├── intentgraph/        # Python Core Engine
├── docs/               # System Documentation
└── infra/              # Terraform & Helm
```

---

## 🚀 Quick Start

### 1. Installation

**Prerequisites:** Node.js 20+, Python 3.10+, Docker.

```bash
# Clone the repository
git clone https://github.com/organization/intentgraph.git
cd intentgraph

# Install Node and Python dependencies
cd apps/api && pnpm install
cd ../web && pnpm install
cd ../../
poetry install
```

### 2. Running Locally

Start the development servers:

```bash
# In terminal 1 (Python Core Engine)
poetry run python -m uvicorn intentgraph.api:app --host 0.0.0.0 --port 8000

# In terminal 2 (Node.js Backend API)
cd apps/api
pnpm run build && node dist/index.js

# In terminal 3 (Frontend)
cd apps/web
pnpm run dev
```

The web dashboard is now available at `http://localhost:5173`. You can log in using `admin@governos.io` and `password`.

### 3. Usage Examples

**API Example:**

```bash
# Login to get token
curl -X POST http://localhost:3001/api/v1/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@governos.io","password":"password"}'

# Submit Intent
curl -X POST http://localhost:3001/api/v1/intents \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <TOKEN>" \
  -d '{"description": "Provision a new S3 bucket for project Alpha"}'
```

---

## ⚙️ Configuration & Deployment

GovernOS is configured via environment variables.

| Variable | Description |
|----------|-------------|
| `JWT_SECRET` | Secret for JWT token generation |
| `PORT` | API listening port |

---

## 🛡️ Security

Security is foundational to GovernOS. Critical workflows are protected by:

- **RBAC & Authorization:** Strict user roles dictate who can plan or approve actions.
- **Sandboxed Execution:** Action plugins operate with least-privilege credentials.
- **Audit Logging:** Every transition is recorded in an immutable event store.

Please read our [Security Policy](SECURITY.md) and [Threat Model](docs/security/threat-model.md) before deploying.

---

## 🗺️ Roadmap

- [x] Python core graph orchestration.
- [x] React 19 / Vite Frontend dashboard.
- [x] Basic execution planning and policy evaluation.
- [x] Fallback SQLite Workflow Engine.
- [x] Connectors for AWS, GCP, and GitHub.
- [x] Role-Based Access Control (RBAC) integration.
- [ ] Implement Temporal durability for long-running workflows.

---

## 🤝 Contributing

We welcome contributions! Please read our [Contributing Guidelines](CONTRIBUTING.md) to get started.

### Development Checks

Before submitting a PR, ensure you run the required checks:

```bash
poetry run pytest tests/
poetry run ruff check .
poetry run mypy .
```

---

## 📄 License & Support

GovernOS is open-source software licensed under the [MIT License](LICENSE).

**Support & FAQ:**
- Check out the [Documentation](docs/).
- Review the [FAQ](docs/troubleshooting/faq.md).
- Open an [Issue](https://github.com/organization/intentgraph/issues).

---

<div align="center">
  <i>Built with precision for the next generation of autonomous systems.</i>
</div>
