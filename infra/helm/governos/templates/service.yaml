apiVersion: v1
kind: Service
metadata:
  name: {{ include "governos.fullname" . }}-api
  labels:
    {{- include "governos.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "governos.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: api
